{
  "name": "uoh-meetings-platform",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/mobile",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "lint:web": "npm --workspace apps/web run lint",
    "dev:mobile": "npm --workspace apps/mobile run start",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "puppeteer": "^24.40.0"
  }
}
